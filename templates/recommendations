<!DOCTYPE html>
<html>
<head>
    <title>Рекомендации VNDB</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
        }
        h1 {
            text-align: center;
            margin-top: 50px;
            color: #333;
        }

        table {
            margin: 30px auto;
            border-collapse: collapse;
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        th, td {
            padding: 10px;
            text-align: left;
        }

        th {
            background-color: #f2f2f2;
        }

        tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        .recommendation-link {
            color: #337ab7;
            text-decoration: none;
        }

        .recommendation-link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <h1>Рекомендации VNDB</h1>
    <table>
        <tr>
            <th>Новелла</th>
            <th>Взвешенный рейтинг</th>
        </tr>
        {% for recommendation in recommendations.iterrows() %}
        <tr>
            <td><a href="https://vndb.org/{{ recommendation[1]['item_id'] }}" class="recommendation-link">{{ recommendation[1]['item_id'] }}</a></td>
            <td>{{ recommendation[1]['Взвешенный рейтинг'] }}</td>
        </tr>
        {% endfor %}
    </table>
</body>
</html>
